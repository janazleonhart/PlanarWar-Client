using UnityEngine;

namespace PlanarWar.Client.UI
{
    public sealed class AppShellController : MonoBehaviour
    {
        // Reserved for the later shell refactor. The first summary shell proof now binds directly through ClientBootstrap.
    }
}
